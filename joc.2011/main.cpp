#include <bits/stdc++.h>
using namespace std;

FILE*fin, *fout;

int n, x, y;
int vb[40'000], vr[40'000];

int main(){
    fin = fopen("joc.in", "r");
    fout = fopen("joc.out", "w");
    fscanf(fin, "%d%d%d", &n, &x, &y);
    for(){}
}