#include <bits/stdc++.h>
using namespace std;

ifstream fin("peste.in");
ofstream fout("peste.out");

int p, n, nrmax = INT_MIN, vf[100];
char v[19], cv[19];

long long

int main() {
    fin >> p >> n;
    for (int i = 1; i <= n; ++i) {
        fin >> v;
        for (int i = 0; i < strlen(v); ++i)
            cv[i] = v[i];
        for (int j = 0; j < strlen(cv)-2; ++j) {
            for (int k = j; k < strlen(cv)-2; ++k) {

            }
        }
    }
}